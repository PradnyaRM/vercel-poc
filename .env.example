# Example environment variables — copy to .env.local for local dev.
# Add these same keys in the Vercel dashboard:
#   Project → Settings → Environment Variables

APP_NAME=Vercel PoC
NEXT_PUBLIC_APP_VERSION=14
